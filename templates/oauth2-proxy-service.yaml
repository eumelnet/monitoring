{{- if index .Values "oauth2-proxy" "enabled" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-oauth2-proxy
  namespace: traefik
spec:
  type: ExternalName
  externalName: {{ .Release.Name }}-oauth2-proxy.{{ .Release.Namespace }}.svc.cluster.local
{{- end }}
